heck if the argument is provided
if ARGV.length != 1
    puts "Usage: #{$PROGRAM_NAME} <string>"
      exit 1
end

# Define the regular expression
# pattern = /School/
#
# # Get the argument from the command line
# input_string = ARGV[0]
#
# # Perform the regular expression match
# result = input_string.scan(pattern)
#
# # Print the result
# puts result.join
